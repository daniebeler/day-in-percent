import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Day in percent',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
